<nav id="navbar" class="navbar navbar-inverse navbar-static-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>"><%= site_title %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to(fa_icon('folder-open', text: 'チャンネル'), channels_path) %></li>
        <% if current_user %>
          <li><%= link_to(fa_icon('cog', text: '管理'), admin_path) %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= fa_icon('user', text: current_user.username) %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to(fa_icon('pencil', text: '利用者情報の編集'), edit_user_path(current_user)) %></li>
              <li role="separator" class="divider"></li>
              <li><%= link_to(fa_icon('sign-out', text: 'ログアウト'), logout_path) %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to(fa_icon('sign-in', text: 'ログイン'), login_path) %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
