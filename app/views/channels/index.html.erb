<h1>チャンネル一覧</h1>
<p>現在 <%= @channels.size %> チャンネルが登録されています。</p>
<table>
  <tr>
    <th>#</th>
    <th>チャンネル名</th>
    <th>識別子</th>
    <th>ログ取得状況</th>
  </tr>
<% @channels.each do |channel| %>
  <tr>
    <td><%= channel.id %></td>
    <td><%= link_to(channel.name_with_prefix, channel) %></td>
    <td><%= channel.identifier %></td>
    <td><%= channel.logging_enabled ? '取得中' : '停止中' %></td>
  </tr>
<% end %>
</table>

