<%= form_for(@channel) do |f| %>
  <div class="form-group">
    <%= f.label(:name) %>
    <%= f.text_field(:name, class: 'form-control', required: true, pattern: '^[^# ,:]+[^ ,:]*$') %>
    <p class="help-block"><strong>先頭の "#" を除いた</strong>チャンネル名を入力してください。例：#もの書き → もの書き</p>
    <p class="help-block">作成後にチャンネル名を変更することはできません。</p>
  </div>

  <div class="form-group">
    <%= f.label(:identifier) %>
    <%= f.text_field(:identifier, class: 'form-control', required: true, pattern: '^[a-z][-_a-z0-9]$') %>
    <p class="help-block">識別子に使用可能な文字は半角英数字（小文字のみ）、ハイフン（<code>-</code>）、アンダーライン（<code>_</code>）です。</p>
  </div>

  <div class="form-group">
    <label class="checkbox-inline">
      <%= f.check_box(:logging_enabled) %>
      ログを記録する
    </label>
  </div>

  <div class="form-group">
    <%= f.submit(t('views.save'), class: 'btn btn-primary') %>
  </div>
<% end %>
