# RAILS_ENV
# Rails の実行環境を指定します。
RAILS_ENV=production

# LOG-ARCHIVER_DATABASE_PASSWORD
# データベースのパスワード
LOG-ARCHIVER_DATABASE_PASSWORD=

# SECRET_KEY_BASE
# セッションを安全に保つための鍵になる文字列です。
# 以下のコマンドを実行することで生成できます。
# bundle exec bin/rails secret
SECRET_KEY_BASE=
